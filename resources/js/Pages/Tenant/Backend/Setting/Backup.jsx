import PortalLayout from "@/Layouts/PortalLayout";
import { Head, Link } from "@inertiajs/react";

export default function Backup({ school }) {
    return (
        <PortalLayout>
            <Head title="Backup Management" />
        </PortalLayout>
    );
}
